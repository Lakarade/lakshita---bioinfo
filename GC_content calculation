#Fuction for calculation of GC content
def gc_content(sequence):

  #Counting G and C nucleotides
  g = sequence.count("G")
  c = sequence.count("C")

  #Calculating G and C percentage
  return (g +c) / len(sequence) * 100

dna = "ATGCCGTACCGTACCTGAACTGCATT"
print(gc_content(dna))
  
